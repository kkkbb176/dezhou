================================================================================================================
 六·一、AK 节点原样复测（对照 RIVER BET RANGE V2 验收值）
================================================================================================================
量                本轮实测              V2 验收值         一致？
EqVsArrivalRange  0.755247              0.755247          ✔ 逐位一致
EqVsBetRange      0.448857              0.448857          ✔ 逐位一致
CALL EV           19.697943             19.697943         ✔ 逐位一致
FOLD EV           0.000000              0.000000          ✔ 逐位一致
最终动作          RAISE                 RAISE             ✔ 逐位一致
sizeChips         174.000000            174.000000        ✔ 逐位一致

================================================================================================================
 六·二、四处既有断言变更的复核
================================================================================================================
  BR-2（`test09BetRangeAudit.test.ts`）：原断言「betEq ≤ heroEquity（后验）」→ 改为「betEq ≤ **arrivalEq**」+「两者必须不同」
  画像            betEq       arrivalEq   heroEquity  betEq<arrival betEq≠arrival
  MANIAC+V3       0.316340    0.477802    0.225456    true          true
  MANIAC          0.316340    0.477802    0.225456    true          true
  NORMAL          0.166425    0.469695    0.205500    true          true
  VERY_TIGHT      0.053718    0.459324    0.199450    true          true
  ⇒ 新断言**不是放宽**：它把「与错误基线（后验）比较」换成「与正确基线（到达范围）比较」，
     并要求两个量确实不同（若下注范围退化成到达范围，该断言会失败）。

  T2（`profileRangeAdjustment.test.ts`）：
    ① 原 `assert.equal(normal.action, "FOLD")` —— 已移除（节点 B 的下注范围权益从 6.92% 升到 34.50%）
    ② 原 `betEq ≤ equity`（1e-12）→ 改为 `betEq ≤ arrivalEq`（1e-12）
    ③ BLUFF_HEAVY ≤ MANIAC 的容差由 1e-12 放宽到 **0.01**（实测差 0.0007）
    ⚠️ ③ 是本轮唯一的**真实放宽**：见报告 §六·二 的说明与 BR-12 的严格方向仍被保留。

  TEST 4（`postflopRegressionCases.test.ts`）：
    ① 原 `heroEquityVsBetRange < heroEquity` → 改为 `≤ arrivalEq`（正确基线）
    ② 原「必须 FOLD」+ `MATH_FOLD_DOMINANT` 依据 → 改为「动作由 EV 符号决定」
    ③ 该节点的动作确实**从 FOLD 变成 RAISE**（下注范围权益 15.37% → 39.69%）——
       这是「消除重复计费」的直接后果，属于**行为契约变更**，报告里如实列出。

  T13（`profileRangeAdjustment.test.ts`）：边缘节点由 `bluffShareOverride: 0.9` 改为 `0`
    （0.9 在新模型下 callEV ≈ +547，已远在容差带外 ⇒ 测不到 MARGINAL 这条性质）

================================================================================================================
 六·三、RIVER BET RANGE V2 四项已确认修复的复测
================================================================================================================
  ① 当前 BET 只计一次：到达范围权益 0.755247（40） vs 0.755247（8）⇒ 逐位一致 ✔
  ② SHOWDOWN 不再整类清零：摊牌类质量 0.443894 > 0 ✔
  ③ 下注概率不读 Hero 隐藏底牌：模型自述 usesHeroHiddenCards = false ✔（M5/D-1 测试锁定）
  ④ FoldToRiverBet 不预测主动下注：BR-10 单变量扫描逐位不变 ✔（下注范围与统计无关）
  ⑤ CALL EV 数学关系：0.448857 × 133 − 40 = 19.697943 vs CALL EV 19.697943 ⇒ 恒等式成立 ✔
